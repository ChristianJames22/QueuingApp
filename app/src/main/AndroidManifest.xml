<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Firebase Metadata -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/logo" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/purple_500" />

        <!-- Splash Activity (Launcher) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Login Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <!-- Sign Up Activity -->
        <activity
            android:name=".SignUpActivity"
            android:exported="true" />

        <!-- Admin Activity -->
        <activity
            android:name=".AdminActivity"
            android:exported="false" />

        <!-- Cashier Activity -->
        <activity
            android:name=".CashierActivity"
            android:exported="false" />

        <!-- Window Selection Activity -->
        <activity
            android:name=".WindowSelectionActivity"
            android:exported="false" />

        <!-- Student Cashier Activity -->
        <activity
            android:name=".StudentCashierActivity"
            android:exported="false" />

        <!-- Student Window Activities -->
        <activity android:name=".StudentWindow1Activity" android:exported="false" />
        <activity android:name=".StudentWindow2Activity" android:exported="false" />
        <activity android:name=".StudentWindow3Activity" android:exported="false" />
        <activity android:name=".StudentWindow4Activity" android:exported="false" />
        <activity android:name=".StudentWindow5Activity" android:exported="false" />
        <activity android:name=".StudentWindow6Activity" android:exported="false" />
        <activity android:name=".StudentWindow7Activity" android:exported="false" />
        <activity android:name=".StudentWindow8Activity" android:exported="false" />

        <!-- Profile Activity -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false" />
    </application>
</manifest>
